# agent_hq

agent_hq is a configuration-driven task engine that runs autonomous engineering work (planning, implementation, review, and merge) as agents inside GitHub Actions, orchestrated entirely from YAML task and config files rather than bespoke workflow code. This repository holds the P0 pilot.
