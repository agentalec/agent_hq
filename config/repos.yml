# Repos config (§6). example-org is a placeholder -- real org/repo names
# await requirements §12 answers.
example-org/product-be:
  role: be
  branch_prefix: agent-hq
  product_area: backend
  base_branch: main
example-org/product-fe:
  role: fe
  branch_prefix: agent-hq
  product_area: frontend
  base_branch: main
