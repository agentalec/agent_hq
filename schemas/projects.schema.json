{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-hq.dev/schemas/projects.schema.json",
  "title": "Projects config",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "engine_repo",
    "final_task",
    "initial_task",
    "intake",
    "intake_label",
    "public",
    "repos"
  ],
  "properties": {
    "repos": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "intake_label": {
      "type": "string"
    },
    "engine_repo": {
      "type": "string",
      "description": "org/repo hosting the engine's own issue tracker (intake, pinned comments, escalations) -- distinct from the work repos in repos.yml"
    },
    "initial_task": {
      "type": "string",
      "description": "Task id enqueued (with the root run's resolved repo) for a newly accepted ticket"
    },
    "feedback_task": {
      "type": "string",
      "description": "Task id enqueued when an approver sends work back with /agent-hq request-changes on a work-repo PR; omit to ignore PR commands entirely"
    },
    "feedback_approvers": {
      "type": "string",
      "description": "approvers.yml group whose members may drive feedback_task from a work-repo PR comment; omit to ignore PR commands entirely"
    },
    "intake": {
      "type": "object",
      "description": "Eligibility filter applied before the first state/artifact write",
      "additionalProperties": false,
      "required": [
        "min_body_words",
        "excluded_labels"
      ],
      "properties": {
        "min_body_words": {
          "type": "integer",
          "minimum": 0
        },
        "excluded_labels": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "public": {
      "type": "boolean",
      "description": "Whether tickets/artifacts may be published (dashboard, public-safe gate)"
    },
    "public_safe_label": {
      "type": "string",
      "description": "Label a ticket must carry before intake accepts it when public is true"
    },
    "final_task": {
      "type": "string",
      "description": "Task id whose completion means the route reached its designed end. Queue-empty completion finishes the ticket only off a terminal run of this task; a queue draining anywhere else blocks the ticket. Mirror of initial_task."
    },
    "comment_default_task": {
      "type": "string",
      "description": "Task queued by a bare (command-less) comment from a feedback_approvers member on the engine issue. Absent means every subject requires an explicit /agent-hq command. Never honoured on a work PR, which is a wider audience."
    }
  },
  "if": {
    "properties": {
      "public": {
        "const": true
      }
    },
    "required": [
      "public"
    ]
  },
  "then": {
    "required": [
      "public_safe_label"
    ]
  }
}
