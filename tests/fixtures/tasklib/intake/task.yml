id: intake
version: 1
description: Intake gate for new tickets
trigger: ticket.labeled
eligibility:
  min_body_words: 5
  excluded_labels:
    - wontfix
budget:
  max_cost_usd: 1
  max_runtime_min: 5
  retries: 0
on_success:
  enqueue:
    - task: spec
