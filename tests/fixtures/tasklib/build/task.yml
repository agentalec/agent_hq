id: build
version: 1
description: Implement the specification
trigger: enqueue
opens_pr: true
budget:
  max_cost_usd: 5
  max_runtime_min: 30
  retries: 1
outputs:
  artifacts:
    - impl/{ticket}.md
handoff:
  allowed:
    - finalize
  max: 1
