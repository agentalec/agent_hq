# Staged: nothing queues it yet -- there is no activation edit, any task in the
# library is queueable. The pilot's spec prompt fans out to implement directly.
id: breakdown
version: 1
description: Break the plan into ordered, per-repo implementation tasks with acceptance-criteria coverage.
trigger: enqueued_by
context:
  - constitution
skills:
  - prompts/breakdown.md
outputs:
  artifacts:
    - specs/{ticket}/tasks.md
budget:
  max_cost_usd: 5
  max_runtime_min: 30
  retries: 2
