id: finalize
version: 1
description: Summarize the ticket outcome.
trigger: enqueued_by
context:
  - specs/{ticket}/*
skills:
  - prompts/finalize.md
outputs:
  artifacts:
    - specs/{ticket}/summary.md
budget:
  max_cost_usd: 2
  max_runtime_min: 15
  retries: 1
